npx lint-staged

# Warn (non-blocking) on files over their CLAUDE.md line-count limit.
files=$(git diff --cached --name-only --diff-filter=ACM -- '*.ts' '*.tsx')
if [ -n "$files" ]; then
  node "$(git rev-parse --show-toplevel)/scripts/check-file-lines.mjs" $files
fi
